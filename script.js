document.addEventListener("DOMContentLoaded", () => {
    // Toda a animação e os efeitos visuais (como o preço e o carrossel) 
    // foram otimizados e estão sendo controlados diretamente pelo CSS.
    console.log("Giardino Segreto - Interface carregada com sucesso!");
});